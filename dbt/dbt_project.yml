name: "grandvista"
version: "1.0.0"
config-version: 2

profile: "grandvista"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  grandvista:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
